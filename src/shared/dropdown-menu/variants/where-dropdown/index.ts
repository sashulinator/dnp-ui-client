import Root, { Contains, EndsWith, Equals, Gt, Gte, Lt, Lte, StartsWith } from './ui/whereDropdownMenu'

const WhereDropdownMenuItem = {
  Root,
  Contains,
  StartsWith,
  EndsWith,
  Equals,
  Gt,
  Gte,
  Lt,
  Lte,
}

export default WhereDropdownMenuItem
